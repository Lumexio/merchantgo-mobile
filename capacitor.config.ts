import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.merchantgo.mobile',
  appName: 'merchantgo-mobile',
  webDir: 'dist'
};

export default config;
